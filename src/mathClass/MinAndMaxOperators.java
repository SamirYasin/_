package mathClass;

import java.lang.reflect.Array;

public class MinAndMaxOperators {
    public static void main(String[] args) {
       /* int maxNumber = Math.max(80, 27);
        int maxNumber2 = Math.max(maxNumber, 45);
        System.out.println("The max number is = " + maxNumber);
        System.out.println(maxNumber2);

        int minNumber = Math.min(80, 27);
        int minNumber2 = Math.min(minNumber, 2);
        System.out.println(minNumber);
        System.out.println(minNumber2);

        int my_array[] = {1,2,3,4};
        int mul = 1;
        for (int i : my_array)
            mul *= i;
        System.out.println("The product is " + mul);

        int maxNumber = Math.max(my_array);
        System.out.println(maxNumber);


        int maxNumber1 = Math.max(6, 9);
        int maxNumber2 = Math.max(17, 49);
        double maxNumber3 = Math.max(34.2, 12.5);
        int maxNumber4 = Math.max(-14, -32);
        int maxNumber5 = Math.max(17, 49);
        int maxNumber6 = Math.max(maxNumber5, 125);

        System.out.println("Max is " + maxNumber1);
        System.out.println("Max is " + maxNumber2);
        System.out.println("Max is " + maxNumber3);
        System.out.println("Max is " + maxNumber4);
        System.out.println("Max is " + maxNumber6);

        int maxNumber1 = Math.max(45, 32);
        int maxNumber2 = Math.max(56, 89);
        System.out.println(Math.max(maxNumber1, maxNumber2));

        */
    }
}
